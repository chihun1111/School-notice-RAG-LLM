# AI Job Challenge 제출용 내용 채움본

## 1. 작품명
학교 공지 게시판 RAG AI 챗봇

## 2. 작품 개요
경동대학교 공개 공지 게시판을 수집해 로컬 데이터셋을 만들고, 학생 질문에 대해 관련 공지를 찾아 출처·날짜·근거 스니펫과 함께 답변하는 Streamlit 기반 RAG 챗봇이다.

## 3. 개발 배경
공지사항은 여러 게시판에 흩어져 있고 제목만 보고 필요한 정보를 찾기 어렵다. 학생은 장학금, 수강신청, 취업 공지처럼 기한이 중요한 정보를 놓칠 수 있다. 본 프로젝트는 공지 검색 시간을 줄이고 신뢰 가능한 근거 기반 답변을 제공하는 것을 목표로 한다.

## 4. 핵심 구현 내용
- `config/boards.yaml`에 승인된 6개 공개 게시판 URL 등록
- `scripts/build_dataset.py`로 수동 데이터셋 갱신
- `data/notices.csv`, `data/notices.jsonl`, `data/crawl_log.jsonl` 생성
- 한국어 문자 n-gram/키워드 기반 하이브리드 검색
- 낮은 신뢰도 질문에 대한 안전 미확인 응답
- Streamlit UI에서 추천 질문, 답변, 원문 링크, 근거 스니펫 표시

## 5. AI 활용 방식
RAG 방식을 사용해 모델이 임의로 답을 만들지 않고, 먼저 공지 데이터셋에서 관련 근거를 검색한다. 이후 검색된 제목·날짜·본문 스니펫만으로 답변을 구성한다. LLM을 연결할 경우에도 프롬프트에는 검색 결과만 제공하고, 근거가 없으면 미확인이라고 답하도록 제한한다.

## 6. 차별점
- 실제 학교 공지라는 생활 밀착 데이터를 활용
- 답변마다 출처와 날짜를 보여 신뢰성 확보
- 외부 API가 없어도 동작하는 안정적인 발표용 구조
- 채팅 중 자동 크롤링을 하지 않아 안전하고 재현 가능

## 7. 검증 계획
```bash
python scripts/build_dataset.py
python scripts/validate_dataset.py
python scripts/smoke_retrieval.py
python -m pytest
streamlit run app.py
```

수동 검증에서는 장학/학사/취업 질문이 관련 공지를 반환하는지, 무관한 질문이 안전 미확인 답변을 반환하는지 확인한다.

## 8. 향후 확장
- 임베딩/벡터DB 기반 의미 검색 추가
- 학과별 공지 필터링
- 관리자 승인 기반 URL 관리
- 공지 요약 리포트 또는 알림 기능
